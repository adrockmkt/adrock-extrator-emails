{
  "title": "Ad Rock Extrator de Emails",
  "description": "Extraia e-mails automaticamente de sites e páginas associadas. Insira uma ou mais URLs e o sistema identificará e-mails válidos em links internos.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "urls": {
      "title": "Lista de URLs",
      "type": "string",
      "editor": "textarea",
      "default": "https://example.com",
      "description": "Insira uma ou mais URLs (uma por linha). Para testes automáticos, um exemplo seguro é https://example.com."
    }
  },
  "required": ["urls"]
}